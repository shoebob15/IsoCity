#include "iso.hpp"
#include "defs.hpp"

int main(int argc, char *argv[]) {
  return initSDL();
  // if ok
  // return EXIT_SUCCESS; I'm not sure if it's a great idea to not return 0 at all in the window
}
